import Link from 'next/link'
export default function AdminInfoPage() {
  return <main className="mx-auto max-w-3xl px-5 py-20 md:px-8"><p className="text-xs uppercase tracking-[.3em] text-primary">Administración</p><h1 className="mt-4 font-serif text-4xl">Panel pendiente de conexión</h1><p className="mt-6 leading-relaxed text-muted-foreground">Esta versión no incluye una base de datos ni un login persistente. Los vídeos se actualizan mediante YouTube y los formularios llegan por correo. Para cambiar imágenes ahora, reemplaza los archivos cuyo nombre comienza por <code>REEMPLAZAR-</code> en <code>public/images</code>.</p><p className="mt-4 leading-relaxed text-muted-foreground">Para disponer de un panel real que edite textos, eventos e imágenes para todos los visitantes es necesario conectar Supabase o un CMS. No se ha simulado un login que solo guarde cambios en un navegador.</p><Link href="/" className="mt-8 inline-block text-primary underline">Volver a la web</Link></main>
}
